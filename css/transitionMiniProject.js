transitionMiniProject.js;
'use strict'
let tileLoad = function () {
    // Variables
    var container = document.getElementsByClassName('container')[0];
    var tule = document.getElementsByClassName('tile')[0];
    var containerWidth = container.scrollWidth;
    var tileWidth = tile.scrollWidth;
    container.classList.append('complete');
    for (let i = 0; i < fullWidth; i ++) {
        
    }

    


};